# 快速开始指南 - Qwen2.5-1.5B GSM8K GRPO训练

本指南专为以下配置定制：
- **模型路径**: `/mnt/data/Qwen2.5-1.5B-Instruct`
- **数据路径**: `/mnt/data/GSM8K`

## 🚀 三步开始训练

### 第一步：检查环境

运行环境检查脚本，确认模型和数据是否就绪：

```bash
cd /home/user/verl
bash check_custom_paths.sh
```

这个脚本会检查：
- ✅ 模型文件是否存在于 `/mnt/data/Qwen2.5-1.5B-Instruct`
- ✅ 数据文件是否存在于 `/mnt/data/GSM8K`
- ✅ Python环境和依赖包是否安装
- ✅ GPU是否可用

### 第二步：准备数据

如果检查脚本提示需要准备数据，运行：

```bash
bash prepare_gsm8k_data_custom.sh
```

这个脚本会：
- 自动识别 `/mnt/data/GSM8K` 中的数据格式
- 支持多种格式：parquet、JSON、Hugging Face格式
- 转换为训练需要的parquet格式
- 保存到 `~/data/gsm8k/` 目录

### 第三步：开始训练

```bash
bash run_qwen2_5-1.5b_gsm8k_grpo_custom.sh
```

## 📋 详细说明

### 如果模型不存在

如果 `/mnt/data/Qwen2.5-1.5B-Instruct` 不存在，你需要先下载模型：

**方法1：使用huggingface-cli（推荐）**
```bash
# 安装huggingface-cli
pip install huggingface-hub[cli]

# 下载模型
huggingface-cli download Qwen/Qwen2.5-1.5B-Instruct \
    --local-dir /mnt/data/Qwen2.5-1.5B-Instruct
```

**方法2：使用git**
```bash
# 需要先安装git-lfs
git lfs install

# 克隆模型
git clone https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct \
    /mnt/data/Qwen2.5-1.5B-Instruct
```

**方法3：使用镜像（国内用户）**
```bash
export HF_ENDPOINT=https://hf-mirror.com

huggingface-cli download Qwen/Qwen2.5-1.5B-Instruct \
    --local-dir /mnt/data/Qwen2.5-1.5B-Instruct
```

### 如果数据不存在

`/mnt/data/GSM8K` 目录应该包含GSM8K数据集，支持以下格式之一：

**格式1：已处理的parquet格式**
```
/mnt/data/GSM8K/
├── train.parquet
└── test.parquet
```

**格式2：JSON格式**
```
/mnt/data/GSM8K/
├── train.json (or train.jsonl)
└── test.json (or test.jsonl)
```

**格式3：Hugging Face格式**
```
/mnt/data/GSM8K/
├── train/
├── test/
└── dataset_info.json
```

**如果目录为空**，数据准备脚本会自动从Hugging Face下载。

### 训练配置调整

打开 `run_qwen2_5-1.5b_gsm8k_grpo_custom.sh`，根据你的GPU调整参数：

**单个16GB GPU（如RTX 3090）**
```bash
export CUDA_VISIBLE_DEVICES=0
NUM_GPUS=1
TRAIN_BATCH_SIZE=4
ROLLOUT_N=4
MINI_BATCH_SIZE=4
GPU_MEMORY_UTIL=0.4
PARAM_OFFLOAD=True
OPTIMIZER_OFFLOAD=True
```

**单个24GB+ GPU（如RTX 4090, A100）**
```bash
export CUDA_VISIBLE_DEVICES=0
NUM_GPUS=1
TRAIN_BATCH_SIZE=16
ROLLOUT_N=5
MINI_BATCH_SIZE=16
GPU_MEMORY_UTIL=0.5
PARAM_OFFLOAD=False
OPTIMIZER_OFFLOAD=False
```

**双GPU**
```bash
export CUDA_VISIBLE_DEVICES=0,1
NUM_GPUS=2
TRAIN_BATCH_SIZE=16
ROLLOUT_N=5
MINI_BATCH_SIZE=16
```

## 🔍 监控训练

### 实时查看日志
```bash
# 查看最新的训练日志
tail -f training_*.log

# 搜索关键指标
grep "reward_mean" training_*.log
grep "loss" training_*.log
```

### 使用Weights & Biases
```bash
# 安装wandb
pip install wandb

# 登录wandb
wandb login

# 训练会自动上传到wandb，在浏览器中查看
```

### 使用nvidia-smi监控GPU
```bash
# 实时监控GPU使用情况
watch -n 1 nvidia-smi
```

## 📁 重要文件位置

### 训练相关
- **训练脚本**: `run_qwen2_5-1.5b_gsm8k_grpo_custom.sh`
- **数据准备**: `prepare_gsm8k_data_custom.sh`
- **环境检查**: `check_custom_paths.sh`

### 数据和模型
- **原始模型**: `/mnt/data/Qwen2.5-1.5B-Instruct`
- **原始数据**: `/mnt/data/GSM8K`
- **处理后数据**: `~/data/gsm8k/`
- **训练日志**: `training_<timestamp>.log`
- **Checkpoints**: `checkpoints/qwen2.5-1.5b-gsm8k-grpo/`

### 文档
- **本指南**: `快速开始指南.md`
- **完整文档**: `QWEN_GSM8K_GRPO_README.md`
- **安装指南**: `VERL_INSTALLATION_GUIDE.md`

## ⚠️ 常见问题

### 1. CUDA Out of Memory

**症状**: `RuntimeError: CUDA out of memory`

**解决方案**:
```bash
# 编辑训练脚本，减小batch size
TRAIN_BATCH_SIZE=2
ROLLOUT_N=3
MINI_BATCH_SIZE=2
GPU_MEMORY_UTIL=0.3
```

### 2. 模型或数据路径错误

**症状**: `FileNotFoundError` 或 `模型目录不存在`

**解决方案**:
```bash
# 确认路径
ls /mnt/data/Qwen2.5-1.5B-Instruct
ls /mnt/data/GSM8K

# 或修改脚本中的路径变量
# 编辑 run_qwen2_5-1.5b_gsm8k_grpo_custom.sh
MODEL_PATH=你的实际路径
```

### 3. 数据格式不对

**症状**: 数据准备脚本报错

**解决方案**:
```bash
# 手动检查数据
ls -lh /mnt/data/GSM8K/

# 如果不确定格式，让脚本自动下载
# prepare_gsm8k_data_custom.sh 会自动从HF下载
```

### 4. verl未安装

**症状**: `ModuleNotFoundError: No module named 'verl'`

**解决方案**:
```bash
# 运行快速安装脚本
bash quick_install_verl.sh

# 或手动安装
cd /home/user/verl
pip install --no-deps -e .
```

## 📊 预期训练效果

### 训练时间
- **单GPU (16GB)**: 每epoch约30-60分钟，总计8-15小时
- **单GPU (24GB+)**: 每epoch约20-40分钟，总计5-10小时
- **双GPU**: 每epoch约15-30分钟，总计4-8小时

### 准确率
- **训练前**: ~50% (基础模型)
- **训练后**: ~65-70% (15 epochs)

### 显存使用
- **单GPU训练**: 约12-14GB
- **双GPU训练**: 每个GPU约8-10GB

## ✅ 完整训练流程

```bash
# 1. 进入工作目录
cd /home/user/verl

# 2. 检查环境
bash check_custom_paths.sh

# 3. 如果verl未安装，先安装
# bash quick_install_verl.sh

# 4. 准备数据（如果需要）
bash prepare_gsm8k_data_custom.sh

# 5. 开始训练
bash run_qwen2_5-1.5b_gsm8k_grpo_custom.sh

# 6. 监控训练（另开终端）
tail -f training_*.log
```

## 🎓 下一步

训练完成后：

1. **查看结果**: 检查日志中的 `test/reward_mean` 指标
2. **导出模型**: 合并LoRA权重为完整模型
3. **测试模型**: 在新的数学问题上测试

---

**祝训练顺利！** 🚀

如有问题，请查看详细文档：`QWEN_GSM8K_GRPO_README.md`
