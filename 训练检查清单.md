# 🚀 Qwen2.5-1.5B GRPO训练检查清单

## ✅ 训练前准备

### 1. 环境检查
- [ ] GPU可用性检查
```bash
nvidia-smi
```

- [ ] 检查Python环境和依赖
```bash
python3 --version
pip list | grep -E "torch|transformers|vllm"
```

- [ ] 检查磁盘空间（至少20GB）
```bash
df -h ~
df -h /mnt/data
```

### 2. 模型文件检查
- [ ] 确认模型已下载到 `/mnt/data/Qwen2.5-1.5B-Instruct`
```bash
ls -lh /mnt/data/Qwen2.5-1.5B-Instruct/
```

应该看到：
- config.json
- model.safetensors 或 pytorch_model.bin
- tokenizer.json
- tokenizer_config.json

### 3. 准备GSM8K数据集
- [ ] 运行数据准备脚本
```bash
cd /home/user/verl
bash prepare_gsm8k_data.sh
```

- [ ] 验证数据文件存在
```bash
ls -lh ~/data/gsm8k/
# 应该看到 train.parquet 和 test.parquet
```

## 🎯 开始训练

### 4. 根据GPU调整配置

编辑 `run_qwen2_5-1.5b_gsm8k_grpo.sh`：

**如果你有1个GPU (16GB显存)**：
```bash
export CUDA_VISIBLE_DEVICES=0
NUM_GPUS=1
TRAIN_BATCH_SIZE=4
ROLLOUT_N=4
MINI_BATCH_SIZE=4
```

**如果你有1个GPU (24GB+显存)**：
```bash
export CUDA_VISIBLE_DEVICES=0
NUM_GPUS=1
TRAIN_BATCH_SIZE=8
ROLLOUT_N=5
MINI_BATCH_SIZE=8
```

**如果你有2个GPU**：
```bash
export CUDA_VISIBLE_DEVICES=0,1
NUM_GPUS=2
TRAIN_BATCH_SIZE=16
ROLLOUT_N=5
MINI_BATCH_SIZE=16
```

### 5. 启动训练
- [ ] 运行训练脚本
```bash
bash run_qwen2_5-1.5b_gsm8k_grpo.sh
```

### 6. 监控训练
- [ ] 查看实时日志
```bash
tail -f training_*.log
```

- [ ] 检查GPU使用情况（另开一个终端）
```bash
watch -n 1 nvidia-smi
```

- [ ] （可选）登录Weights & Biases查看训练曲线
```bash
# 首次使用需要登录
wandb login
```

## 📊 关键指标

训练过程中关注以下指标：

1. **reward_mean**: 平均奖励，越高越好（GSM8K中，接近1表示答案基本正确）
2. **actor/loss**: Actor损失，应该逐渐下降
3. **actor/kl_loss**: KL散度，保持在合理范围（不要太大）
4. **rollout/response_length**: 生成的回答长度

## 🐛 故障排查

### OOM (Out of Memory)错误
```bash
# 减小batch size
TRAIN_BATCH_SIZE=2
MINI_BATCH_SIZE=2
ROLLOUT_N=3

# 降低GPU显存使用
actor_rollout_ref.rollout.gpu_memory_utilization=0.3
```

### 训练速度慢
```bash
# 如果显存充足，可以：
# 1. 增加gpu_memory_utilization到0.5-0.6
# 2. 关闭offload（如果显存够用）
actor_rollout_ref.actor.fsdp_config.param_offload=False
actor_rollout_ref.actor.fsdp_config.optimizer_offload=False
```

### 网络问题（下载数据集失败）
```bash
# 使用Hugging Face镜像
export HF_ENDPOINT=https://hf-mirror.com
bash prepare_gsm8k_data.sh
```

## 📈 预期训练时间

根据配置不同，训练时间约：

- **单GPU (16GB)**: 每个epoch约30-60分钟，总计8-15小时
- **单GPU (24GB+)**: 每个epoch约20-40分钟，总计5-10小时
- **双GPU**: 每个epoch约15-30分钟，总计4-8小时

## ✅ 训练完成

checkpoint保存在：
```
/home/user/verl/checkpoints/<project_name>/<experiment_name>/
```

查看最终结果：
```bash
# 查看最后几行日志
tail -100 training_*.log

# 搜索最终验证结果
grep "test/reward" training_*.log | tail -5
```

---

**祝训练顺利！** 🎉
